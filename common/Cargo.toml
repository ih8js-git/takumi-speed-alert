[package]
name = "common"
version = "0.1.0"
edition = "2024"

[dependencies]
bincode = { workspace = true }
rstar = { workspace = true, features = ["serde"] }
serde = { workspace = true, features = ["derive"] }
