import { Palette, VerticalBox } from "std-widgets.slint";

export component PostDownloadPage inherits VerticalBox {
    alignment: center;

    Text {
        text: "Download Complete!";
        font-size: 24px;
        horizontal-alignment: center;
        color: Palette.foreground;
    }
}
