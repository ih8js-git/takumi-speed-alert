import { Palette, VerticalBox } from "std-widgets.slint";

export component CompletePage inherits VerticalBox {
    alignment: center;

    Text {
        text: "Installation Complete!";
        font-size: 24px;
        horizontal-alignment: center;
        color: Palette.foreground;
    }
}
